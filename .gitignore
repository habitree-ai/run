# 로컬 캡쳐 인박스 (서빙용 복사본은 images/)
기록/

# 개인 메모 (공개 repo 제외)
DOC/

# 요청 백로그 (로컬 전용 — 커밋하지 않음. 지우면 이력이 복구되지 않으니 삭제 금지)
.backlog/

# 환경값 기록 (커밋 금지)
.env.local
.env*.local

# Vercel
.vercel/

# 블로그 자동 발행 스크립트 산출물 (로그인 세션·스크린샷 — 커밋 금지)
scripts/node_modules/
scripts/.naver-profile/
scripts/out/
scripts/package-lock.json

# OS
Thumbs.db
.DS_Store
.playwright-mcp/

.vercel
